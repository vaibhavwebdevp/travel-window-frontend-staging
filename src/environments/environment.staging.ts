export const environment = {
  production: false,
  apiUrl: 'https://travel-window-backend-staging.vercel.app/api', // replaced via build script
  recaptchaSiteKey: '' // replaced via build script
};
